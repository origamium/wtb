PORT=4000
WS_PORT=4001
LOG_LEVEL=info
DEPRECATED_FLAG=1
NODE_ENV=development

# A quoted value containing '#' — wtb keeps it intact (the '#' is NOT treated as
# an inline comment). Regression smoke test for the .env parser.
DB_URL="postgres://app:p@ss#word@localhost:5432/app_db"
